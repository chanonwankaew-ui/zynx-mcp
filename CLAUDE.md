# Zynx MCP Workspace Notes

- Treat `src/` as the production TypeScript MCP wrapper and local backend.
- Treat `apps/workflow-mapper/` and `apps/blueprint-explorer/` as separate app surfaces.
- Keep generated output, local SDKs, `.env`, and machine-specific editor files out of git.
- Preserve Thai product terminology where it carries domain meaning.
